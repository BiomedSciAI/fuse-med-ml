dist: xenial

language: python

python:
  - "3.7"

before_install:
  - pip install --upgrade pip
  - pip install --upgrade setuptools
  - pip install --upgrade importlib_metadata
  - pip list

install:
  - pip install --progress-bar off -r requirements.txt
  - pip install --progress-bar off -r dev_requirements.txt

script:
# Static type checking
#   GOAL:
  # - python -m mypy fuse fuseimg examples --show-error-codes
  # - python -m black fuse --check --diff --color
  # - python -m flake8 . 

#  Current
  - python -m mypy examples/fuse_examples/imaging/classification/isic --show-error-codes
  - python -m black examples/fuse_examples/imaging/classification/isic --check -- diff --color
  - python -m flake8 examples/fuse_examples/imaging/classification/isic